[package]
name = "grass"
version = "0.1.0"
edition = "2021"

[dependencies]
log = "0.4.14"
env_logger = "0.9.0"

tokio = { version = "1.14.0", features = ["sync", "net", "rt", "macros", "rt-multi-thread"] }

uuid = { version = "0.8.2", features = ["v4"] }